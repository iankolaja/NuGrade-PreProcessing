# Core dependencies — stages 1 and 3, run_pipeline, and every validation script.
#
#     pip install -r requirements.txt
#
# Deliberately tiny. The usual cluster job is stage 1, which needs nothing else from pip;
# putting the embedding stack in here made `pip install -r requirements.txt` resolve torch,
# transformers and spaCy together, which is a multi-gigabyte download and long enough on a
# slow link to look like a hang.
#
# For stage 2 (report embedding) also install:
#     pip install -r requirements-embedding.txt
#
# OpenMC is needed only by stage 1, to read ACE evaluation files, and is not a pip package:
#     conda install -c conda-forge openmc
# It is imported lazily, so everything except the cross-section reads works without it.

numpy>=1.24
pandas>=2.0
